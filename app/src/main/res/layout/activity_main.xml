<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="12dp"
        android:background="#0A0A0A">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="SOLANACY"
                android:textColor="#00E5A0"
                android:textSize="14sp"
                android:letterSpacing="0.15"
                android:fontFamily="monospace"
                android:textStyle="bold"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="FOUNDER AI AGENT v1.1"
                android:textColor="#445544"
                android:textSize="9sp"
                android:letterSpacing="0.1"
                android:fontFamily="monospace"/>
        </LinearLayout>

        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="● OFFLINE"
            android:textColor="#FF4444"
            android:textSize="10sp"
            android:fontFamily="monospace"
            android:textStyle="bold"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="5dp"
            android:paddingBottom="5dp"
            android:background="@drawable/status_bg"
            android:letterSpacing="0.08"/>
    </LinearLayout>

    <!-- System Info Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#050505"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp">

        <TextView
            android:id="@+id/tvSystemInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ARIA v1.1 — Solanacy Technologies"
            android:textColor="#1A4A1A"
            android:textSize="9sp"
            android:fontFamily="monospace"/>
    </LinearLayout>

    <!-- Terminal -->
    <ScrollView
        android:id="@+id/terminalScroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#000000"
        android:padding="12dp"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Boot text -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="╔════════════════════════════════╗\n║  SOLANACY FOUNDER AI v1.1      ║\n║  Saumik Paul — CEO             ║\n║  ARIA — Agentic Runtime AI     ║\n╚════════════════════════════════╝"
                android:textColor="#00E5A0"
                android:textSize="11sp"
                android:fontFamily="monospace"
                android:paddingBottom="8dp"/>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="$ system ready. tap mic to connect."
                android:textColor="#336633"
                android:textSize="11sp"
                android:fontFamily="monospace"
                android:paddingBottom="12dp"/>

            <TextView
                android:id="@+id/tvTerminal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#00E5A0"
                android:textSize="11sp"
                android:fontFamily="monospace"
                android:lineSpacingExtra="3dp"/>
        </LinearLayout>
    </ScrollView>

    <!-- Progress Bar -->
    <View
        android:id="@+id/progressBar"
        android:layout_width="0dp"
        android:layout_height="2dp"
        android:background="#00E5A0"
        android:visibility="gone"/>

    <!-- Bottom Bar -->
    <LinearLayout
        android:id="@+id/bottomBar"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:background="#0A0A0A"
        android:paddingStart="24dp"
        android:paddingEnd="24dp">

        <!-- Mic Button -->
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="24dp">

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/btnMic"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@android:drawable/ic_btn_speak_now"
                app:backgroundTint="#00E5A0"
                app:tint="#000000"
                app:fabSize="normal"/>
        </FrameLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnDisconnect"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="DISCONNECT"
            android:textColor="#FF4444"
            android:textSize="11sp"
            android:fontFamily="monospace"
            android:letterSpacing="0.08"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            app:strokeColor="#FF4444"
            app:cornerRadius="4dp"/>

    </LinearLayout>

</LinearLayout>
